# Trending AI Repos — 2026-08-20

> status: partial (blocked at step 1 — GitHub search API unreachable this session)

## Editor's take
This session's GitHub access remains scoped to the `jerbud308/trending-github-repositories` repo only, so none of the six queries in `inputs/sources.md` could be run against `api.github.com/search/repositories` or its MCP equivalent — the same restriction that blocked the 08-14, 08-17, and 08-19 briefs. That makes four blocked runs out of the last six scheduled days, against one working run (08-18) and one day (08-16) with no brief at all, so this is no longer an isolated hiccup with a working run in between — it now looks like the dominant state. Per the standing rule against inventing stars, dates, or unverifiable repos, this brief ships empty rather than backfilled; a human should check whether the session's GitHub scope is meant to be this narrow, since as configured it structurally prevents the routine from doing its job most days.

## Top 10 — last 7 days
No entries — the `ai-7d`, `llm-7d`, `agents-7d`, `mlops-7d`, and `rag-7d` queries could not be executed (API access blocked, see status note above).

## Top 5 — last 30 days
No entries — the `ai-30d` query could not be executed (API access blocked, see status note above).

---
*Generated by the Trending GitHub Repos routine.*
